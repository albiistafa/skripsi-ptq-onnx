# virtualenvs (besar, dibuat ulang via venv + pip)
venv_onnx/
venv_tf/

# python
__pycache__/
*.pyc

# dataset gambar aligned LFW (103MB, reproducible via align; pairs.txt tetap di-track)
dataset/lfw_aligned/
data/

# log eval mentah
results/eval_log.txt



.DS_Store

# gambar hasil ekstraksi .bin (reproducible via scripts/extract_bin.py)
dataset/*_bin/
dataset/lfwbin/
dataset/lfw-aligned/
